// Client-safe exports (components, types)
// Dashboard currently renders inline in the page — no extracted components yet.
export type { DashboardData, DashboardStats, RecentMovement, LowStockItem } from "./server";
